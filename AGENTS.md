# AGENTS.md — 구현 AI 작업 규칙 (vibe-kit)

> 이 파일은 이 저장소에서 코드를 만지는 구현 AI(OpenAI Codex CLI 등)가 지킬 규칙이다.
> vibe-kit 의 코딩 규칙만 골라 미러링했다 — **세부·최신 규칙의 원본은 `.claude/CLAUDE.md` · `docs/plan/` · `.claude/skills/handoff/SKILL.md`(분업 절차) 이다.** 이 파일과 원본이 다르면 원본이 이긴다.
> 작업은 보통 `docs/plan/handoffs/` 의 작업지시서 한 장으로 들어온다. 지시서가 있으면 그 범위 밖은 건드리지 않는다.

## 코딩 4원칙 (Karpathy) — 원본: `.claude/CLAUDE.md` §코딩 4원칙

1. **가정하지 말고 물어라.** 요구가 애매하면 멈추고 되묻는다. 조용히 넘겨짚고 밀어붙이지 마라. 해석이 여럿이면 다 제시하고 사용자가 고르게 한다.
2. **최소 코드.** 시킨 문제를 푸는 가장 작은 코드만. 요청 안 한 기능·"나중을 위한" 유연성·안 일어날 상황의 예외 처리 금지.
3. **시킨 것만 고쳐라(수술적).** 인접 코드·주석·포매팅을 "개선"하지 마라. 안 망가진 걸 리팩토링하지 마라. 무관한 데드 코드는 발견해도 건드리지 말고 말만 해라.
4. **검증까지 반복.** "고쳤다"로 끝내지 말고, 실제로 되는지 확인(화면에서 눌러보기·테스트)할 때까지 스스로 돈다. 증거 없이 완료라 말하지 마라.

## 작업 방식 — 원본: `.claude/CLAUDE.md` §작업 방식

- **먼저 `plan.md` 를 읽어라.** 블럭 순서대로, 한 번에 한 스텝(체크 한 칸)만 만든다. 스텝이 끝나면 줄 끝의 "— 확인:" 방법대로 검증한다.
- **체크박스 규칙**: 지시서(`docs/plan/handoffs/`)로 들어온 작업이면 **지시서 안의 체크박스만 갱신하고 `plan.md` 체크박스는 건드리지 마라** — plan.md 체크는 리뷰 통과 후 Claude 쪽에서 한다.
- 막히면 **"더 작게 쪼개기"**. 큰 걸 통째로 시도하지 마라.
- **화면을 만들거나 고칠 땐 그 화면의 `docs/plan/screens/*.md` 를 먼저 읽어라** (있으면). 거기 적힌 **예외 5종(비었을 때·로딩·실패·권한 없음·잘못 입력)까지 처리해야 완성**이다 — 성공 경로만 만들고 끝내지 마라.
- **방향을 정하거나 바꾸는 결정**(기능 빼기·스택 교체·권한/삭제 정책 변경)을 하면 `docs/plan/06-decisions.md` 에 4줄(결정·왜·버린 대안·영향)로 기록한다.
- **다 만든 뒤의 큰 변경은 제안서 먼저.** 새 기능 추가·기존 동작 변경·표 구조 변경·연동 추가라면 코드를 만지기 전에 `docs/plan/changes/날짜-제목.md` 로 변경 제안서(① 뭘 바꾸나 ② 왜 ③ 영향 ④ 스텝 계획)를 쓰고 사용자 승인을 받는다. 승인 후 빌드하고, 끝나면 06-decisions.md 에 4줄 요약을 남기고 plan.md 에 블럭·스텝을 추가한다. **제안서 파일은 지우지 않는다 — 그게 변경 이력이다.** (오타·문구·한 줄 버그 수정은 제안서 없이 바로.)

## 스택 규칙 — 공통 (모든 프로젝트) — 원본: `.claude/CLAUDE.md` §스택 규칙

- 비교는 `===` / `!==` 만. `==` / `!=` 금지. null 은 명시적으로 확인.
- 키는 항상 `.env` 에서 읽는다. 코드·깃·로그에 키를 박거나 노출하지 마라. `.env` 파일 자체를 읽거나 출력하지도 마라.
- **공식 API 가 없는 서비스는 스크래핑·비공식 자동화로 잇지 마라** (계정 정지·법적 위험). 미개방이면 대안(수동 입력·공식 채널·다른 서비스)을 제시하고 멈춘다.
- **N+1 금지**: 목록을 그릴 때 항목마다 저장소를 또 부르지 마라 — 필요한 걸 한 번에 모아 읽는다.

## 스택 규칙 — 트랙별 (이 프로젝트에 해당하는 쪽만 적용) — 원본: `.claude/CLAUDE.md` §스택 규칙

어느 트랙인지는 기획서(`plan.md` 헤더·`docs/plan/01-prd.md`)의 스택 표기를 따른다.

**풀스택 트랙(Next.js + Supabase)이면:**
- 웹 = `apps/web` (Next.js App Router). 저장이 필요하면 `packages/integrations/supabase` 를 재사용한다 — 새로 만들지 마라. Supabase 키 이름은 `SUPABASE_URL` / `SUPABASE_ANON_KEY`.
- **`NEXT_PUBLIC_` 접두사 금지.** 환경변수를 새로 만들 때도 붙이지 마라. 브라우저에서 읽을 공개 키는 `apps/web/next.config.mjs` 의 `env` 목록으로만 처리한다.
- DB 관계는 **꼬리표 컬럼으로만, FOREIGN KEY 제약은 걸지 않는다** (정합성은 soft delete + 앱 로직). FK 제약이 없으면 중첩 조회가 안 되니 조인은 두 번 나눠 읽거나 뷰로. 검색·정렬·걸쇠 컬럼엔 인덱스를 걸어라.
- 테이블 생성·변경 SQL 은 전문을 만들어 보여주기만 한다 — 실행은 사용자가 Supabase 대시보드 SQL Editor 에서 하고, 실행 후 Table Editor 에서 표가 생겼는지 사용자가 눈으로 확인하게 하라. Supabase CLI·Vercel CLI 설치를 권하지 마라.
- RLS·입력검증·XSS 는 개발 중 강제하지 않는다 — 공개 배포 전 Claude 쪽 security-reviewer 가 점검한다. 임의로 개발 단계에 RLS 를 켜지 마라.

**정적 트랙(바닐라 JS + localStorage + 정적 배포)이면:**
- 프레임워크·빌드 도구·번들러를 새로 들이지 마라. 지금 구조 그대로 간다.
- localStorage 는 한 번 읽어 메모리에서 다룬다 — 루프 안에서 매번 `JSON.parse` 하지 마라 (N+1 의 로컬판).

## 보안 — 치명 3종 (사용자가 안 시켜도 자동 적용) — 원본: `.claude/CLAUDE.md` §보안

1. **시크릿 노출**: 키는 `.env` 에만 (`.gitignore` 에 `.env` 확인). `SUPABASE_SERVICE_ROLE_KEY` 같은 마스터 키는 절대 클라이언트(브라우저 코드·공개 env 목록)에 두지 마라 — 서버에서만. (반면 `SUPABASE_ANON_KEY` 는 공개 전제 키라 노출돼도 됨.)
2. **남의 데이터 파괴**: 사용자 데이터 표에 공개(anon) 삭제·수정 정책을 열지 마라. (읽기 공개는 개발 중 허용 — 이게 디버깅을 편하게 한다.) 삭제는 되도록 soft delete(`deleted_at`). 앱 안의 '전체 삭제·초기화' 기능은 확인 절차(되묻기) 없이 넣지 마라.
3. **결제·인증 사고**: 결제·인증은 테스트 키/테스트 모드로 먼저 만든다. 실키 전환은 맨 마지막.

파괴적 명령(rm -rf, DB 삭제·TRUNCATE·대량 UPDATE)은 사용자 확인 없이 실행 금지. 특히 실서비스 DB.

## 분업 경계 — 원본: `.claude/skills/handoff/SKILL.md`

이 저장소에선 기획·리뷰는 Claude Code 가, 구현은 이 파일을 읽는 네가 맡는다. 너는 지시서 범위의 코드와 검증 결과로만 말한다. 구현이 끝나면 **커밋하지 말고 멈춰라** — git diff 리뷰(code-reviewer)는 Claude 쪽 사이클이 돌고, 커밋은 리뷰 통과 후다.
